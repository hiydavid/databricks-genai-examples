[project]
name = "genie-migration"
version = "0.1.0"
description = "Databricks Genie Space migration tools"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "databricks-sdk>=0.76.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
]
