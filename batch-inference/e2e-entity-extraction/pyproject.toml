[project]
name = "e2e-entity-extraction"
version = "0.1.0"
description = "End-to-end medical claims entity extraction pipeline on Databricks"
requires-python = ">=3.11"
dependencies = [
    "mlflow>=3.8.1",
    "databricks-sdk>=0.40.0",
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
]
